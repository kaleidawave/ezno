There are several kinds of expression types

- `null`
- `undefined`
- Numbers
- Strings
- Booleans

### Escape sequences handled

```ts
const x: "\"" = '"';
const y: "Hi" = `Hello\``;
```

- Type "Hello`" is not assignable to type "Hi"
